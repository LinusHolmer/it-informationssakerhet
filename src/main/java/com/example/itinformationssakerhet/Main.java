package com.example.itinformationssakerhet;

public class Main {
    public static void main(String[] args) {

        /*
        Book book = new Book()
                .setYear(1)
                .setAuthor("me")
                .setTitle("hello");

        Computer computer = new Computer.Builder()
                .setCpu("Intel i7")
                .setRam(16)
                .setStorage(512)
                .setGraphicsCard("Nvidia RTX 3060")
                .build();



        MessagePrinter printer = new StandardPrinter();
        printer.print("Hej från en vanlig klass!");

        MessagePrinter printer1 = new MessagePrinter() {
            @Override
            public void print(String message) {
                System.out.println("Anonym: " +message);
            }
        };
        printer1.print("Hej från en anonym klass!");

        MessagePrinter printer2 = message -> System.out.println("Lambda: " +message);
        printer2.print("Hej från en lambda klass?");

         */
    }
}
